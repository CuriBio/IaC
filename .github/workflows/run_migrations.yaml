name: 'Run Database Migration'
on:
  workflow_dispatch:
    inputs:
      workspace:
        description: 'Enter workspace'
        required: true
        default: ''
  pull_request:
    branches-ignore:
      - main

jobs:
  migrations:
    name: 'Run database migration'
    strategy:
      fail-fast: false
      matrix:
        os:
          - 'ubuntu-20.04'
        python-version:
          - 3.9

    runs-on: ${{ matrix.os }}

    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      TF_VAR_user_id: ${{ github.actor }}
      AWS_DEFAULT_REGION: us-east-1
      AWS_DEFAULT_OUTPUT: json
      WORKSPACE: ${{ github.event.inputs.workspace }}

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: 'Setup Python'
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Display Python version
      run: |
        python -c "import sys; print(sys.version)"
        python -m venv venv

    - name: Activate virtual environment (Linux/MacOS)
      if: runner.os == 'Linux' || runner.os == 'macOS'
      run: . venv/bin/activate

    - name: Display Pip Version and confirm environment empty
      run: |
        pip -V
        pip freeze

    - name: Cache Python dependencies
      uses: actions/cache@v2.1.4
      env:
        cache-name: cache-pip
      with:
        path: ~/.cache/pip
        key: ${{ matrix.os }}-${{ matrix.python-version }}-build-${{ env.cache-name }}-${{ hashFiles('requirements*.txt') }}
        restore-keys: |
          ${{ matrix.os }}-${{ matrix.python-version }}-build-${{ env.cache-name }}-

    - name: Install Dev Dependencies
      run: pip install -r requirements-dev.txt

    - name: Log full installed packages
      run: pip freeze

    - name: 'Terraform setup'
      uses: hashicorp/setup-terraform@v1
      with:
        terraform_version: 0.14.7
        terraform_wrapper: false

    - name: 'Create Environment'
      id: tfenv
      run: python ./.github/scripts/tf-deploy-env.py --workspace test --no-color

    - name: Access Terraform outputs
      run: |
          OUTPUTS=$(python ./.github/scripts/tf-deploy-env.py --workspace "iac-${{ github.event.pull_request.number }}" --output)
          echo $OUTPUTS
          ASSUMED_ROLE_ARN=arn:aws:iam::077346344852:role/terraform_deploy_role
          DB_KEY_ARN=arn:aws:secretsmanager:us-east-1:077346344852:secret:db-ec2-key-pair-aofYm8
          DB_CREDS_ARN=arn:aws:secretsmanager:us-east-1:077346344852:secret:db-creds-WszNCl
          DB_HOST=iac-329-mantarray-rds.cluster-cya18gqzi4zd.us-east-1.rds.amazonaws.com
          EC2_HOST=ec2-user@jump.curibio-test.com

    - name: Run migration script
      run: |
        chmod +x ./.github/scripts/run_migration.sh
        ./.github/scripts/run_migration.sh $ASSUMED_ROLE_ARN $DB_KEY_ARN $DB_CREDS_ARN $DB_HOST $EC2_HOST
